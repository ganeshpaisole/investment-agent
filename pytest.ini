[pytest]
# Restrict test discovery to the main test directories to avoid duplicate imports
# (there are duplicate test files under proposal_agent/proposal_agent/tests).
testpaths =
    proposal_agent/tests
    nse_agent
python_files = test_*.py
