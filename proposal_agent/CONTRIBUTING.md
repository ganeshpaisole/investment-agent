Contributing to Proposal Agent

- Run `dev_setup.py` to create the .venv and install dev dependencies.
- Write tests for any new functionality and run `pytest`.
- Keep KB clause files under `kb/clauses` and include front-matter metadata for tags and jurisdiction where applicable.
- Commit small, focused PRs. CI will run tests on push.
